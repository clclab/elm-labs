version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

# don't fail on warnings
# sphinx:
#   fail_on_warning: true

formats:
  - pdf

python:
  install:
    - requirements: docs/requirements.txt